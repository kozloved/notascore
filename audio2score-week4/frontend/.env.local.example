# Same-origin /api (proxied to FastAPI via app/api/[...path] and next.config
# rewrites). On Railway set BACKEND_URL=http://backend.railway.internal:$PORT
NEXT_PUBLIC_API_URL=/api
BACKEND_URL=http://127.0.0.1:8000

export const metadata = {
  title: "Check your email",
  description: "Verify your NotaScore email address.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

export const metadata = {
  title: "Forgot password",
  description: "Reset your NotaScore password.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}

export const metadata = {
  title: "Pricing",
  description: "NotaScore plan structure. Prices will be published when billing opens.",
};

export default function PricingLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}

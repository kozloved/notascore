# setuptools<81 keeps pkg_resources, which resampy (a basic-pitch dep) imports at runtime.
setuptools<81

fastapi
uvicorn
python-multipart
python-dotenv
sqlalchemy
redis
# rq<2 keeps the rq.Connection API used by worker.py.
rq<2

# Local MVP storage uses disk; add supabase/psycopg2 later if needed.
basic-pitch
onnxruntime
music21
pretty_midi
librosa
soundfile
numpy
pytest
# Beat / downbeat (meter). PyPI 0.16.1 breaks on Python 3.11 + modern NumPy.
# Cython is required to build madmom from git.
Cython
madmom @ git+https://github.com/CPJKU/madmom.git
# Optional AudioSet instrument tagging (not beat tracking):
#   pip install tensorflow-hub          # YAMNet
#   pip install panns-inference         # PANNs CNN14 (PyTorch, ~300MB weights)


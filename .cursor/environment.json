{
  "name": "NotaScore (Audio2Score)",
  "build": {
    "dockerfile": "Dockerfile"
  },
  "install": "bash .cursor/install.sh",
  "start": "bash .cursor/start.sh",
  "terminals": [
    {
      "name": "api",
      "command": "cd audio2score-week4/backend && .venv/bin/uvicorn main:app --host 0.0.0.0 --port 8000",
      "description": "FastAPI backend: upload, job status and MusicXML result endpoints on http://localhost:8000"
    },
    {
      "name": "worker",
      "command": "cd audio2score-week4/backend && .venv/bin/python worker.py",
      "description": "RQ worker that runs the Basic Pitch transcription engine"
    },
    {
      "name": "frontend",
      "command": "cd audio2score-week4/frontend && npm run dev",
      "description": "Next.js dev server (upload UI) on http://localhost:3000"
    }
  ],
  "ports": [
    { "name": "frontend", "port": 3000 },
    { "name": "api", "port": 8000 }
  ]
}
